---
description: Run one My AX improvement campaign through production proof
argument-hint: "[focus]"
---
Read `loops/SPEED.md` and execute it${1:+ with focus: $@}.

Arm a Terraloop, lock the contract from SPEED.md, create a `loops_task` driver, then work. Default stop is **one** shareable feature deployed and proved. Land a second only when it is already bounded and disjoint. A focus that names one bug or security fix is one proved iteration.

Do not launch a 6–12 scout swarm. Name the atoms. Scout only the independent ones (usually 2–4). Parent does the rest. Terrarium is a lever, not the default workplace.

Before selecting work: local My AX evidence first, then production receipts, then one external or `cfi` scan if the finding touches Cloudflare packages. Freeze the user-outcome block in SPEED.md. Weak user benefit → no-change.

You are the parent: verify the starting revision; ride callbacks; independently re-run cited proofs; integrate, deploy through the owner wrappers, and prove the exact journey; repair or roll back until proof passes; require one independent stop-gate auditor PASS; delete the driver.
